# Environment — .env.example IS committed; .env and overrides are not
.env
.env.local
.env.*.local

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.uv/

# Data (raw corpus, derived reviews, generated briefs, schedule state, logs — never in git)
data/raw/
data/processed/
data/briefs/
data/schedule/
data/logs/

# Node
node_modules/
web/node_modules/
web/dist/
web/.vite/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Scratch UI screenshots + design exploration artifacts (dev-only, not committed)
*.png
!docs/screenshots/*.png          # README dashboard screenshots ARE committed
.playwright-mcp/
htmlref.txt
mockups/
web/public/mockup.html

# Personal Claude Code settings (project agents + CLAUDE.md ARE committed)
.claude/settings.local.json

# SAM build artifacts + vendored Lambda deps (regenerated by `make build`)
infra/.aws-sam/
infra/lambdas/pipeline/
infra/lambdas/reliability/
samconfig.toml
